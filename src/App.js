import React from 'react';
import ReactDOM from 'react-dom';

const app = () => {
    return (
        <div>
            <p>React Trial gone wrong!</p>
        </div>
    );
};

export default app;

ReactDOM.render(<App />, document.getElementById('app'));